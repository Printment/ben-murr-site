import { SimplePage } from "@/components/simple-page"

export const metadata = { title: "CV" }

export default function CvPage() {
  return (
    <SimplePage
      eyebrow="Page placeholder"
      title="CV page coming next."
      body="This placeholder keeps the top-level navigation working while the full professional profile page is still being written."
    />
  )
}
